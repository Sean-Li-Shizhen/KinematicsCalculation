<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yahoo.stringsean.kinematicscalculation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViVfTActivity" />
        <activity android:name=".ViVfAActivity" />
        <activity android:name=".ViVfdXActivity" />
        <activity android:name=".ViTAActivity" />
        <activity android:name=".ViTdXActivity" />
        <activity android:name=".ViAdXActivity" />
        <activity android:name=".VfTAActivity" />
        <activity android:name=".VfTdXActivity" />
        <activity android:name=".VfAdXActivity" />
        <activity android:name=".TAdXActivity"></activity>
    </application>

</manifest>